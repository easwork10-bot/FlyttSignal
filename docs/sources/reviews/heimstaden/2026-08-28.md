# Heimstaden Uppsala — public listing page

Reviewed: 2026-08-28

Maintenance check: 2026-09-12. The city landing page no longer contains listing
cards. The adapter now reads `https://heimstaden.com/se/sok-lagenhet/?text=Uppsala`
and parses `.object-card` elements with `data-object-id`. The previous IDs and
normalized field names are retained. The public response embeds other cities too;
only Uppsala cards qualify. An empty result requires both the Uppsala search field
and the explicit zero-match counter; unknown markup still fails.

Live verification: SUCCESS, zero Uppsala matches, zero additions/removals; all seven
stored listings retained. Inventory completeness remains unverified (`INCOMPLETE`),
so this response cannot authorize removals. 362 backend tests and Ruff passed.
The worker was rebuilt and deployed. No new live listing was available to verify
new-signal creation during this check. The review below describes the older page.

## Purpose and scope

Observe non-personal, publicly displayed rental facts for Uppsala: stable object identifier, canonical detail URL, street address, rent, rooms, area and availability date. Interest registration, accounts, applicant data, contact-person data and authenticated pages are out of scope.

## Observed interface

The public city page rendered three apartment cards during the controlled review:

- `1238103-1401`, Kryddblandargatan 49, 3 rooms, 71 m², SEK 14,032/month;
- `1238104-1001`, Kryddblandargatan 47, 1 room, 29 m², SEK 7,611/month;
- `1238108-1202`, Kryddblandargatan 39, 1 room, 29 m², SEK 7,285/month.

All three displayed availability on 2026-12-01. The card element exposes a stable-looking compound object ID and a canonical `/se/sok-lagenhet/Uppsala-…/` detail URL. A controlled parser run reproduced all selected fields.

The page is bounded but is not proven to be a complete inventory. It links onward to the larger search experience and can present a changing subset. M3.1 therefore records successful live runs as `INCOMPLETE` with a known-partial-scope reason. It may create/update observations but cannot produce removal events.

## Access and operational controls

- Public HTML, no login or security bypass.
- `robots.txt` does not disallow the reviewed public path; it only disallows `/wp-admin/` and publishes the Swedish sitemap.
- No public site-wide reuse licence or automation-specific terms were located during this review. Privacy and cookie pages concern visitors and applicants, not a listing-data licence.
- Only non-personal property facts are retained; no applicant or employee/contact data is collected.
- Live mode has a dedicated kill switch, identifying user agent, 20-second timeout, redirect handling, a 25-item hard cap, Uppsala-only validation and fail-closed schema checks.
- Default runtime mode is the dated repository fixture. One controlled live parser run passed; recurring live collection remains disabled.

## Provenance and overlap

Every observation uses `upstream_provider_key=heimstaden`, including objects published through Uppsala Bostadsförmedling. Domains are publisher channels, not independent providers. The direct page and Bostadsförmedling were observed to have partial rather than complete overlap, so both channels remain useful while contributing only one provider identity to scoring.

## Decision

- Fixture and controlled parser verification: `GO`.
- Recurring live polling: `WAIT` for an explicit operational/reuse position and a deliberate polling schedule.
- Removal detection from this interface: `REJECT` because completeness cannot be established.

## Evidence

- https://heimstaden.com/se/lediga-lagenheter-uppsala/
- https://heimstaden.com/robots.txt
- https://heimstaden.com/se/integritetspolicy/
- https://heimstaden.com/se/cookiepolicy/
- https://heimstaden.com/se/newsroom/heimstaden-samarbetar-med-uppsala-bostadsformedling/
